namespace Minigames.Contract
{
    public class MinigameResult
    {
        // Состояния "провал" не существует — только успех.
        // Payload оставлен как точка расширения (очки/собранное для флейвора).
        public object Payload;
    }
}
